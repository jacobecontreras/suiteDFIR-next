//! Locating the libimobiledevice tools (the bundled sidecar directory passed in by the shell, or the
//! system `PATH` on Linux) and their hashes; `list_devices`, `device_info`, `pair`/`validate`,
//! `will_encrypt` and `set_encryption`, with output parsing per docs/IDEVICE-CLI.md.
//!
//! Implemented by ROADMAP task X2.
