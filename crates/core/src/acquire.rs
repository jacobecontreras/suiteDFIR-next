//! One acquisition end-to-end (ARCHITECTURE.md §6b) via a callback: `acquisition.json` lifecycle,
//! discovery and recovery (`CaseDetail.acquisitions`), preflight, the backup process, progress and
//! prompt parsing, validation and sealing; encryption enable, restore and later restore.
//!
//! Implemented by ROADMAP tasks X3a and X3b.
