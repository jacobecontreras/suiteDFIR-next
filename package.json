{
  "name": "suitedfir",
  "version": "0.2.0",
  "private": true,
  "description": "suiteDFIR UI dev tooling (type checking and tests). No runtime packages.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "node --test \"tests/ui/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
